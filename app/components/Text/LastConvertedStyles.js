import EStyleSheet from 'react-native-extended-stylesheet';

const LastConvertedStyles = EStyleSheet.create({
    smallText: {
        color: '$white',
        fontSize: 12,
        textAlign: 'center'
    }
});

export default LastConvertedStyles;